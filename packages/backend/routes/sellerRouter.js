const express = require('express');
const router = express.Router();
const UserFromDB = require('../models/user_model');
const PetFromDB = require('../models/pet_model'); // Pet model
// const authMiddleware = require('../middleware/authMiddleware'); // Ensure authenticated requests




module.exports = router;